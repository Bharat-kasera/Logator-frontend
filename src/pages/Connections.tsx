import React from 'react';
import { Box, Typography } from '@mui/material';

const Connections: React.FC = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4" gutterBottom>
        Connections
      </Typography>
      {/* Connections content will go here */}
    </Box>
  );
};

export default Connections;
